'use strict';

/**
 * investor-sub-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::investor-sub-type.investor-sub-type');
